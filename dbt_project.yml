name: nhl_analytics

profile: default

model-paths: ["models"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  nhl_analytics:
    staging:
      +schema: STAGING
    intermediate:
      +schema: INTERMEDIATE
    